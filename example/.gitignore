# written by `make setup` from your uid/gid
.env

# generated by `make gen-charts`. In a real project you would commit these
# along with the sqitch.plan entry that references them; they are left out
# here so the example starts from a clean slate.
sqitch/deploy/statechart/
sqitch/revert/statechart/
sqitch/verify/statechart/
